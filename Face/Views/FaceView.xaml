<UserControl x:Class="Face.Views.FaceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:Face.Views"
             mc:Ignorable="d" 
           d:DesignHeight="870" d:DesignWidth="1920">
    <UserControl.Resources>
        <ResourceDictionary>
            
            <Style x:Key="localBtn"  TargetType="Button">
                <Setter Property="Margin" Value="15,10"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Template">

                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" CornerRadius="3" Padding="8" Background="{TemplateBinding Background}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                               />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="border" Property="RenderTransform">
                                        <Setter.Value>
                                            <ScaleTransform ScaleX="1.05"  ScaleY="1.05"/>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="PageButtonStyle" TargetType="Button">
                <Setter Property="Width" Value="40"/>
                <Setter Property="Height" Value="40"/>
                <Setter Property="Margin" Value="2"/>
                <Setter Property="BorderBrush" Value="LightGray"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Foreground" Value="Gray"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="pageborder" CornerRadius="5" Background="White" BorderBrush="{TemplateBinding BorderBrush}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="pageborder" Property="Opacity" Value="0.8"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="pageborder" Property="RenderTransform">
                                        <Setter.Value>
                                            <ScaleTransform ScaleX="1.05"  ScaleY="1.05"/>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="ActivePageButtonStyle" TargetType="Button" BasedOn="{StaticResource PageButtonStyle}">
                <Setter Property="Background" Value="#3498db"/>
                <Setter Property="Foreground" Value="Green"/>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Button Command="{Binding NavigateCommand}" CommandParameter="IndexView" Content="返回" Background="Red" Width="100"  FontSize="18" Height="46" Margin="0,0,37,0"  HorizontalAlignment="Right"/>
        <TextBlock Grid.Row="1" FontFamily="SourceHanSansCN-Bold" Foreground="White" FontSize="31px"  Margin="376,0,0,21" Text="人员列表"/>
        <Border Background="White" CornerRadius="10"   VerticalAlignment="Top" Width="1126" Height="660"  Grid.Row="2"  >
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <DockPanel LastChildFill="False">
                    <Button Content="+新增用户"  Background="#0177D5 " Style="{StaticResource localBtn}"/>
                    <Grid Width="300" Height="50">
                        <!-- 圆角背景（可选） -->
                        <Border CornerRadius="30" Background="#F5F5F5" BorderBrush="LightGray" BorderThickness="2"/>

                        <!-- 搜索框主体 -->
                        <TextBox  x:Name="SearchTextBox"
         materialDesign:HintAssist.Hint="搜索"
         Style="{StaticResource MaterialDesignOutlinedTextBox}"
         BorderThickness="0"
         Background="Transparent"
         Width="300"
         Height="50"
         Padding="15,0,50,0" 
         VerticalContentAlignment="Center"
         FontSize="18">
                            <TextBox.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="25"/>
                                </Style>
                            </TextBox.Resources>
                        </TextBox>

                        <!-- 搜索按钮 -->
                        <Button Command="{Binding SearchCommand}"
        HorizontalAlignment="Right"
        Style="{StaticResource MaterialDesignIconButton}"
        Foreground="#2196F3" 
        Margin="0,0,10,0">
                            <materialDesign:PackIcon Kind="Magnify" Width="33" Height="33"/>
                        </Button>
                    </Grid>
                    <Button Content="删除" Background="Red" Padding="20" DockPanel.Dock="Right" Style="{StaticResource localBtn}"/>
                    <Button Content="导入人员" Background="#F56E25" Style="{StaticResource localBtn}"  DockPanel.Dock="Right" />
                    <Button Content="导出人员" Background="#0177D5 " Style="{StaticResource localBtn}" DockPanel.Dock="Right"/>
                </DockPanel>
                <DataGrid ItemsSource="{Binding FaceDtos}" 
          AutoGenerateColumns="False"
          Grid.Row="1"
          Margin="20,10" ClipToBounds="True"
                          VerticalContentAlignment="Stretch" VerticalAlignment="Stretch"
          CanUserAddRows="False"
    CanUserDeleteRows="False"
    CanUserReorderColumns="True"
    CanUserResizeColumns="True"
    CanUserResizeRows="False"
    CanUserSortColumns="True"
    SelectionMode="Extended"
    SelectionUnit="FullRow"
          IsReadOnly="True"
          AlternatingRowBackground="#FFF5F5F5"
          RowBackground="White"
          GridLinesVisibility="None"
          HeadersVisibility="Column"
          FontSize="14"
          HorizontalGridLinesBrush="#FFE0E0E0"
          VerticalGridLinesBrush="#FFE0E0E0"
                        
          ColumnWidth="*">
  
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="Auto" MinWidth="50"/>
                        <DataGridTextColumn Header="姓名" Binding="{Binding Name}" Width="*" MinWidth="100"/>
                        <DataGridTextColumn Header="性别" Binding="{Binding Sex}" Width="Auto" MinWidth="60"/>
                        <DataGridTextColumn Header="工号" Binding="{Binding WorkId}" Width="Auto" MinWidth="80"/>
                        <DataGridTextColumn Header="工作" Binding="{Binding WorkName}" Width="*" MinWidth="120"/>
                        <DataGridTextColumn Header="创建时间" Binding="{Binding CreateDate, StringFormat=yyyy-MM-dd HH:mm}" Width="Auto" MinWidth="150"/>
                        <DataGridTemplateColumn Width="120" IsReadOnly="True">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <!-- 编辑按钮 -->
                                        <Button Content="编辑" 
                            Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                            CommandParameter="{Binding}"
                            Margin="2" Padding="5 2"
                            Style="{StaticResource MaterialDesignFlatButton}"/>

                                        <!-- 删除按钮 -->
                                        <Button Content="删除" 
                            Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                            CommandParameter="{Binding}"
                            Margin="2" Padding="5 2"
                            Style="{StaticResource MaterialDesignFlatButton}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>

                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Height" Value="Auto"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#FFE3F2FD"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#FFBBDEFB"/>
                                    <Setter Property="BorderBrush" Value="#FF90CAF9"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>

                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Height" Value="40"/>
                            <Setter Property="Background" Value="#FF2196F3"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="#FF1976D2"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>
                </DataGrid>
                <StackPanel Margin="10" Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
                    <!-- 上一页按钮 -->
                    <Button x:Name="BtnPrev" Content="&lt;" Style="{StaticResource PageButtonStyle}" IsEnabled="False"/>

                    <!-- 页码按钮（动态生成） -->
                    <Button Content="1" Style="{StaticResource ActivePageButtonStyle}"/>
                    <Button Content="2" Style="{StaticResource PageButtonStyle}"/>
                    <Button Content="3" Style="{StaticResource PageButtonStyle}"/>
                    <TextBlock Text="..." VerticalAlignment="Center" Margin="5,0"/>
                    <Button Content="20" Style="{StaticResource PageButtonStyle}"/>

                    <!-- 下一页按钮 -->
                    <Button x:Name="BtnNext" Content="&gt;" Style="{StaticResource PageButtonStyle}"/>

                    <!-- 总页数和跳转输入框 -->
                    <TextBlock Text="共20页" VerticalAlignment="Center" Margin="10,0,5,0"/>
                    <TextBlock Text="到" VerticalAlignment="Center" Margin="5,0"/>
                    <TextBox x:Name="TxtPage" Width="50" VerticalAlignment="Center">
                       
                    </TextBox>
                    <TextBlock Text="页" VerticalAlignment="Center" Margin="5,0"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
