<UserControl x:Class="Face.Views.Dialog.AddFaceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:Face.Views.Dialog"
             mc:Ignorable="d" 
             Width="500" Height="350">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <TextBlock Margin="20" Text="注册用户" FontSize="22"/>
        <StackPanel HorizontalAlignment="Left" Margin="10,20" Grid.Row="1">
            <TextBox Text="{Binding CurrentDto.Name}" materialDesign:HintAssist.Hint="请输入姓名" Width="200" Margin="10,5"/>
            <ComboBox Text="{Binding CurrentDto.Sex}" materialDesign:HintAssist.Hint="请输入姓名" SelectedIndex="0" Width="200" Margin="10,5">
                <ComboBoxItem>男</ComboBoxItem>
                <ComboBoxItem>女</ComboBoxItem>
            </ComboBox>
            <TextBox Text="{Binding CurrentDto.WorkId}" materialDesign:HintAssist.Hint="请输入工号" Width="200" Margin="10,5"/>
            <TextBox Text="{Binding CurrentDto.WorkName}" materialDesign:HintAssist.Hint="请输入工作" Width="200" Margin="10,5"/>
        </StackPanel>
        <StackPanel Margin="20" Height="40" Width="160"  Orientation="Horizontal" HorizontalAlignment="Right"  Grid.Row="2" >
            <Button Command="{ Binding CancelCommand}" Style="{StaticResource MaterialDesignOutlinedButton}" Margin="10,0" Content="取消"/>
            <Button Command="{Binding SaveCommand}" Style="{StaticResource MaterialDesignFlatLightBgButton}" Content="确定"/>
        </StackPanel>
    </Grid>
</UserControl>
